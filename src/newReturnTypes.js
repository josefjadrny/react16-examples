import React from 'react'

const NewReturnTypes = () => {
  return [
    <h1>New return types</h1>,
    <p>You can new use arrays as a return type in render method</p>,
  ]
}

export default NewReturnTypes